on: pull_request

name: Pull requests

jobs:
  blockAutosquashCommits:
    name: Block Autosquash Commits

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master

      - name: Block Autosquash Commits
        uses: ./
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
