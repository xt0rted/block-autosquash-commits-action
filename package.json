{
  "name": "block-autosquash-commits-action",
  "private": true,
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "actions-toolkit": "^2.1.0"
  },
  "devDependencies": {
    "eslint": "^6.1.0"
  }
}
