{
  "name": "block-autosquash-commits-action",
  "private": true,
  "main": "main.js",
  "scripts": {
    "lint": "eslint ./",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "ncc build main.js"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^7.11.0",
    "@vercel/ncc": "^0.34.0"
  }
}
