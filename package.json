{
  "name": "block-autosquash-commits-action",
  "private": true,
  "main": "main.js",
  "scripts": {
    "lint": "eslint ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^7.14.0"
  }
}
